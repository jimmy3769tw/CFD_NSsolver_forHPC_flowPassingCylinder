#pragma once
// * stl
#include <string>
#include <vector>       // std::vector 
                        // std::size since -> -c++=17
                        // std::data since -> -c++=17
                        // std::begin std::end since -> -c++=11
                        // std::cbegin std::cend since -> -c++=14

#include <cmath>
#include <stdexcept>    // std::exception  invalid_argument ...
#include <unistd.h>     // for print pid
#include <tuple>        // std::tuple, // -c++=-11
#include <utility>      // std::pair, std::make_pair
#include <algorithm>
#include <iterator>
#include <numeric>
#include <functional>
#include <unordered_set>
#include <random>
#include <cstdlib>
#include <map>
#include <cfloat>
#include <memory>


// stream
#include <iostream>
#include <fstream>